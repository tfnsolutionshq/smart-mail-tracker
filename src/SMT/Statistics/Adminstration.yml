info:
  name: Adminstration
  type: http
  seq: 6

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}users/statistics"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
