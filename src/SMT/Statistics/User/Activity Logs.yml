info:
  name: Activity Logs
  type: http
  seq: 2

http:
  method: GET
  url: "{{SETTING_BASE_URL}}logs/me"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
