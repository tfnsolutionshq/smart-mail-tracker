info:
  name: Track Memo
  type: http
  seq: 1

http:
  method: GET
  url: "{{MEMO_BASE_URL}}external-memos/tracking/EXT-2026-001"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
