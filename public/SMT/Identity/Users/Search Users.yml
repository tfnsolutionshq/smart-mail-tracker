info:
  name: Search Users
  type: http
  seq: 18

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}users/search?q=promise"
  params:
    - name: q
      value: promise
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
