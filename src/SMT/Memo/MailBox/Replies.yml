info:
  name: Replies
  type: http
  seq: 11

http:
  method: GET
  url: "{{MEMO_BASE_URL}}mailbox/9b9c10fc-c01c-43e7-b4f1-fa94761f8c73/replies"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
