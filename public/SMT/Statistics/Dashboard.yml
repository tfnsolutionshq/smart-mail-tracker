info:
  name: Dashboard
  type: http
  seq: 4

http:
  method: GET
  url: "{{MEMO_BASE_URL}}dashboard/statistics"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
