info:
  name: Overview Trend
  type: http
  seq: 3

http:
  method: GET
  url: "{{MEMO_BASE_URL}}reports/memo-volume-trend"
  params:
    - name: category_id
      value: "2"
      type: query
      disabled: true
    - name: start_date
      value: 2024-01-01
      type: query
      disabled: true
    - name: end_date
      value: 2024-12-31
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
