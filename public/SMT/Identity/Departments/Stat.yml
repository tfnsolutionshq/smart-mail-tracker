info:
  name: Stat
  type: http
  seq: 7

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}departments/statistics"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
