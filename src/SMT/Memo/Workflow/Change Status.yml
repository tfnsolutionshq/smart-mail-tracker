info:
  name: Change Status
  type: http
  seq: 6

http:
  method: PUT
  url: "{{MEMO_BASE_URL}}workflows/019ac9cb-773b-706b-abd5-aa6b4df14600/toggle-status"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
