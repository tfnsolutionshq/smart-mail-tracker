info:
  name: Get Settings
  type: http
  seq: 1

http:
  method: GET
  url: "{{MEMO_BASE_URL}}notification-settings"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
