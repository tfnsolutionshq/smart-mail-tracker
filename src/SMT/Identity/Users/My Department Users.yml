info:
  name: My Department Users
  type: http
  seq: 6

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}users/my-department"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
