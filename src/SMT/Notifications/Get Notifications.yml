info:
  name: Get Notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{NOTIFICATION_BASE_URL}}notifications/019b220d-4d9d-73eb-9667-cdf8ac228944?limit=5"
  params:
    - name: limit
      value: "5"
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
