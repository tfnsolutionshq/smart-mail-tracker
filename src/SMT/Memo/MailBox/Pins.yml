info:
  name: Pins
  type: http
  seq: 9

http:
  method: GET
  url: "{{MEMO_BASE_URL}}mailbox/pinned"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
