info:
  name: Get memos
  type: http
  seq: 3

http:
  method: GET
  url: "{{MEMO_BASE_URL}}external-memos"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
