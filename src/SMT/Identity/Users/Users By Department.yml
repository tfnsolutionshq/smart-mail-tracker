info:
  name: Users By Department
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}users/departments/admin"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
