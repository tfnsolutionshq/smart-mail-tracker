info:
  name: System Memos
  type: http
  seq: 4

http:
  method: GET
  url: "{{MEMO_BASE_URL}}admin/memos"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
