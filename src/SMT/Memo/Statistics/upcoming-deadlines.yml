info:
  name: upcoming-deadlines
  type: http
  seq: 4

http:
  method: GET
  url: "{{MEMO_BASE_URL}}dashboard/upcoming-deadlines?per_page=4"
  params:
    - name: per_page
      value: "4"
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
