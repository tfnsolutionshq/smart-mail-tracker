info:
  name: Reset password Request
  type: http
  seq: 5

http:
  method: POST
  url: "{{LOCAL_BASE_URL}}reset-password-request"
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: elexispd@gmail.com
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
