info:
  name: Mailbox Statistics
  type: http
  seq: 2

http:
  method: GET
  url: "{{MEMO_BASE_URL}}dashboard/mailbox-statistics"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
