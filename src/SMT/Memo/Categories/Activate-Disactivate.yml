info:
  name: Activate/Disactivate
  type: http
  seq: 4

http:
  method: POST
  url: "{{MEMO_BASE_URL}}categories/7/toggle-status"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
