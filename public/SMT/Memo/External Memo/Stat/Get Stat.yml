info:
  name: Get Stat
  type: http
  seq: 1

http:
  method: GET
  url: "{{MEMO_BASE_URL}}external-memos/stats"
  body:
    type: json
    data: "{\r

      \    \"comment\": \"Testing comment\"\r

      }"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
