info:
  name: Memo Stat
  type: http
  seq: 1

http:
  method: GET
  url: "{{MEMO_BASE_URL}}dashboard/user?per_page=5"
  params:
    - name: per_page
      value: "5"
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
