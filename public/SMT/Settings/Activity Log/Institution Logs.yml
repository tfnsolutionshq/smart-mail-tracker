info:
  name: Institution Logs
  type: http
  seq: 2

http:
  method: GET
  url: "{{NOTIFY_BASE_URL}}logs?per_page=20"
  params:
    - name: per_page
      value: "20"
      type: query
  body:
    type: json
    data: "// {\r

      //     \"user\": \"2e160ea0-a953-45ea-9090-f6b3243b6526\",\r

      //     \"per_page\": 3\r

      // }"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
