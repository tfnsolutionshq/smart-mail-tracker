info:
  name: All Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{MEMO_BASE_URL}}categories"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
