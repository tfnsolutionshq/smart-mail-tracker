info:
  name: Get all Depts.
  type: http
  seq: 4

http:
  method: GET
  url: "{{identity_base_url}}departments"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
