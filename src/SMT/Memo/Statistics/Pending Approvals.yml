info:
  name: Pending Approvals
  type: http
  seq: 3

http:
  method: GET
  url: "{{MEMO_BASE_URL}}dashboard/pending-approvals"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
