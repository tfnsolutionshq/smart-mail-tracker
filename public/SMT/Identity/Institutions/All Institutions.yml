info:
  name: All Institutions
  type: http
  seq: 1

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}institutes"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
