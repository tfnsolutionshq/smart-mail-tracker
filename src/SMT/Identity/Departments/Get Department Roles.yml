info:
  name: Get Department Roles
  type: http
  seq: 6

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}departments/019abf45-6675-71a4-a92c-50e51b46e0ff/roles"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
