info:
  name: Create Department Role
  type: http
  seq: 4

http:
  method: POST
  url: "{{identity_base_url}}roles/department"
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: Bursary
      - name: description
        type: text
        value: Pens Dept Activities
      - name: ""
        type: text
        value: ""
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
