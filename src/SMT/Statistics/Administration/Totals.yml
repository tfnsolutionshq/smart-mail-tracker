info:
  name: Totals
  type: http
  seq: 3

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}analytics/totals"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
