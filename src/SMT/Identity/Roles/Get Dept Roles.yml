info:
  name: Get Dept Roles
  type: http
  seq: 5

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}roles/my-department"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
