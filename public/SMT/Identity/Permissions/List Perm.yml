info:
  name: List Perm
  type: http
  seq: 4

http:
  method: GET
  url: "{{LOCAL_BASE_URL}}permissions"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
