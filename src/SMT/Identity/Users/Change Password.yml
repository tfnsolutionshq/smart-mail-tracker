info:
  name: Change Password
  type: http
  seq: 15

http:
  method: POST
  url: "{{LOCAL_BASE_URL}}change-password"
  body:
    type: json
    data: "{\r

      \  \"current_password\": \"password\",\r

      \  \"new_password\": \"88888888\",\r

      \  \"new_password_confirmation\": \"88888888\"\r

      }"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
